//DJC 2nd April 2000package uk.ac.bham.cs.djc.ui;import java.applet.Applet;import java.awt.*;import java.awt.event.*;import java.net.URL;import java.net.MalformedURLException;/**	Applet to demonstrate pitch and goalpost detection on a single image * 	 * 	<a href="FrameAnalyserApplet.java">Source code</a> * 	@author David Chatting (djc@cs.bham.ac.uk) *	@version Created 2nd April 2000 */public class FrameAnalyserApplet extends Applet {    /**	The panel containing displaying the processed image */    private FrameAnalyserPanel imagePanel;        /**	The image to process */    private Image image;        /**	Initial the applet - open an image to process     */    public void init() {	try {	    	//Get the applet parameters:	    	int numberOfFiles=(new Integer(getParameter("numberOfFrames"))).intValue();		String path=getParameter("frameDirectory");				//Open a random image from the specified directory:	    	int randomFile=(int)(Math.random()*(double)numberOfFiles);		String urlAsString=new String(path + (new Integer(randomFile)).toString() + ".gif");		URL imageURL=new URL(urlAsString);		image = getImage(imageURL);	}	catch(Exception e) {	}    }        /**	Start the applet     */    public void start() {	setLayout(new GridLayout(1,1));		//Add the panel showing the processed image:	imagePanel = new FrameAnalyserPanel(image);        add(imagePanel);		try {	    	//Parse the image:		imagePanel.parse();		setVisible(true);	}	catch(Exception e) {	   	System.out.println(e);	}	    }}